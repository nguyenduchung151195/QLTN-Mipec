/*
 * EditStockPage Messages
 *
 * This contains all the text for the EditStockPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditStockPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditStockPage container!',
  },
});
