/*
 * AssetGuaranteePage Messages
 *
 * This contains all the text for the AssetGuaranteePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetGuaranteePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetGuaranteePage container!',
  },
});
