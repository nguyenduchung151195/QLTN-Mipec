/*
 * AddDocumentary Messages
 *
 * This contains all the text for the AddDocumentary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddDocumentary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddDocumentary container!',
  },
});
