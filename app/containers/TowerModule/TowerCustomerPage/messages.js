/*
 * TowerCustomerPage Messages
 *
 * This contains all the text for the TowerCustomerPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TowerCustomerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TowerCustomerPage container!',
  },
});
