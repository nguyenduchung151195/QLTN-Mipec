/*
 * TowerVenderPage Messages
 *
 * This contains all the text for the TowerVenderPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TowerVenderPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TowerVenderPage container!',
  },
});
