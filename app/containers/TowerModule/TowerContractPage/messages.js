/*
 * TowerContractPage Messages
 *
 * This contains all the text for the TowerContractPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TowerContractPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TowerContractPage container!',
  },
});
